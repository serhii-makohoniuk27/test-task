# AI Usage Reflection

- Used GitHub Copilot (GPT-5.3-Codex) to speed up refactoring for response shaping and cleanup logic.

- Adapted generated suggestions to meet take-home requirements: grouped category output, `dish_name` key for CLI output, and simple text cleanup.

- Added and adjusted tests after each change to verify behavior and prevent regressions.
